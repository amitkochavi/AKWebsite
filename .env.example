# Password to access the content dashboard at /admin
ADMIN_PASSWORD=changeme

# Secret used to sign admin session cookies (use a long random string in production)
ADMIN_SECRET=replace-with-a-long-random-string
